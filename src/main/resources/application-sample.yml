spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: [ url ]
    username: [ username ]
    password: [ password ]

  flyway:
    enabled: true
#    enabled: false
    baseline-on-migrate: true
    url: [ url ]
    user: [ username ]
    password: [ password ]